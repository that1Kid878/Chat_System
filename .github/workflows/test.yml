name: Run tests

on: [push, pull_request]

env:
  Database_URL: 'sqlite:///./test.db'
  ACCESS_SECRET_KEY: '7abbee9cf8f18ea0c1a60b6905cd412e'
  REFRESH_TOKEN_PEPPER: '47c01147a0d22fc9aa036f5c9886cb5a'
  

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pytest
        run: python -m pytest
